name: generate manual graphs

on:
  schedule:
    - cron: "15 0 * * *"
  workflow_dispatch:

jobs:
  generate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: generate bomberman SVGs
        uses: abozanona/pacman-contribution-graph@main
        with:
          github_user_name: ${{ github.repository_owner }}
          games: 'bomberman'

      - name: generate snake SVGs
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg
            dist/github-snake-dark.svg?palette=github-dark

      - name: organize into subfolder
        shell: bash
        run: |
          mkdir -p output-files/graphs
          mv dist/*.svg output-files/graphs/ 2>/dev/null
          mv bomberman-contribution-graph*.svg output-files/graphs/ 2>/dev/null

      - name: push to output branch
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./output-files
          publish_branch: output
          keep_files: true
